#include <algorithm>
#include <iostream>
#include <bitset>
#include <queue>
const int MAXN = 0;
using namespace std;
queue<int> que;
bitset<MAXN> que;
void bfs()
{
    while (que.empty() == false)
    {
        
    }
    return;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
}